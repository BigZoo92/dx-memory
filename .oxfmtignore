# oxfmt ignore — passed explicitly via `--ignore-path` so oxfmt does NOT fall back
# to `.prettierignore` (which excludes the whole Flow tree from Prettier and would
# otherwise hide it from oxfmt too).

# Generated by TanStack Router on build — never hand-formatted.
**/routeTree.gen.ts
