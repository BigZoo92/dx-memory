{
  "$comment": "Single source of truth for the metric catalog and the derived scores. Every collected metric key MUST appear in `metrics`. Score groups reference metric keys and weights. Normalization is min-max across the three variants (see tools/metrics/score.mjs); unavailable metrics are skipped and their group's weights are renormalized so partial data never fakes a score.",
  "version": "2.0.0",
  "modelVersion": "1.0",
  "$comment_freeze": "MODEL FROZEN for the experiment (modelVersion 1.0, 2026-07-05) after the final coherence audit: axis semantics, members and weights are fixed. Collection BUG fixes remain allowed; any semantic change to what is scored or how requires bumping modelVersion. The defense scores must be reproducible by this exact model.",
  "normalization": "ratio-to-best-across-variants",
  "headline": {
    "method": "weighted-mean-over-measured-axes",
    "$comment": "Total Delivery Score = weighted mean of the delivery axes that are actually measured for a variant (gated axes are excluded and the remaining weights renormalized). Change is weighted highest because change/maintenance is the majority of a product's lifetime cost. Normalization is ratio-to-best (see score.mjs): each metric's cheapest variant sets the bar at 100 and the others score proportionally — with only three data points, min-max forced the worst variant to 0 and erased the difference between a 5% gap and a 5× gap."
  },
  "axisCoverageMin": 0.34,
  "balanceBands": {
    "$comment": "For structural metrics both extremes are costly: too few projects/files means an under-modularized monolith (hard to change safely), too many means over-fragmentation (hard to hold in your head). Bands are engineering heuristics for a product of this size, applied identically to all variants. Score = 100 inside [idealMin,idealMax], falling linearly to 0 at hardMin/hardMax.",
    "nxProjects": { "idealMin": 6, "idealMax": 18, "hardMin": 1, "hardMax": 40 },
    "graphNodes": { "idealMin": 6, "idealMax": 18, "hardMin": 1, "hardMax": 40 },
    "sourceFiles": { "idealMin": 55, "idealMax": 220, "hardMin": 5, "hardMax": 400 },
    "locTotal": { "idealMin": 4000, "idealMax": 9500, "hardMin": 500, "hardMax": 20000 }
  },
  "axisWeights": {
    "$comment": "Weights of the 4 delivery axes inside the headline. Change dominates at 0.50: it is where lifetime delivery cost concentrates (most of a product's life is spent modifying it, not building it the first time). Build carries 0.20 (the feedback loop is paid dozens of times a day), Ship and Run 0.15 each. When an axis has no data it gates out and the headline renormalizes over the measured axes.",
    "Build": 0.2,
    "Ship": 0.15,
    "Run": 0.15,
    "Change": 0.5
  },
  "metrics": {
    "nxProjects": { "label": "Workspace projects", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "balance", "collector": "graph", "description": "Buildable projects (npm packages + Rust crates). Scored on balance: a monolith is under-modularized, dozens of projects is over-fragmented; the healthy band is in between." },
    "sourceFiles": { "label": "Source files", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "balance", "collector": "architecture", "description": "Source files (ts/tsx/js/rs/css), excluding tests, config, docs and generated code. Scored on balance." },
    "locTotal": { "label": "Total LOC", "category": "Architecture", "axis": "Change", "unit": "loc", "direction": "balance", "collector": "architecture", "description": "Non-empty lines of source code. Scored on balance — too little means missing structure, too much means bloat." },
    "locFrontend": { "label": "Frontend LOC", "category": "Architecture", "axis": "Change", "unit": "loc", "direction": "neutral", "collector": "architecture", "description": "Lines of front-end code (heuristic classification)." },
    "locBackend": { "label": "Backend LOC", "category": "Architecture", "axis": "Change", "unit": "loc", "direction": "neutral", "collector": "architecture", "description": "Lines of back-end / server code (heuristic classification, includes Rust)." },
    "locTests": { "label": "Test LOC", "category": "Architecture", "axis": "Change", "unit": "loc", "direction": "neutral", "collector": "architecture", "description": "Lines of test code." },
    "locDocs": { "label": "Docs LOC", "category": "Architecture", "axis": "Change", "unit": "loc", "direction": "neutral", "collector": "architecture", "description": "Lines of Markdown documentation." },
    "configFiles": { "label": "Config files", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "architecture", "description": "Number of configuration files (json/yaml/toml/rc/*.config.*)." },
    "todoCount": { "label": "TODO / FIXME", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "architecture", "description": "Occurrences of TODO / FIXME / HACK / XXX markers in source." },
    "anyCount": { "label": "`any` usages", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "architecture", "description": "Uses of the TypeScript `any` escape hatch (`: any`, `as any`, `<any>`)." },
    "asUnknownAsCount": { "label": "`as unknown as` casts", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "architecture", "description": "Double casts through `unknown` — a strong smell of fighting the type system." },
    "lintDisableCount": { "label": "Lint disables", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "architecture", "description": "eslint-disable / oxlint-disable / @ts-ignore / @ts-expect-error directives." },
    "avgComplexity": { "label": "Avg cyclomatic complexity", "category": "Architecture", "axis": "Change", "unit": "index", "direction": "lower", "collector": "architecture", "description": "Mean per-function cyclomatic complexity (decision-point heuristic)." },
    "maxComplexity": { "label": "Max cyclomatic complexity", "category": "Architecture", "axis": "Change", "unit": "index", "direction": "lower", "collector": "architecture", "description": "Worst single-function cyclomatic complexity found." },
    "fnsOverComplexity": { "label": "Functions over threshold", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "architecture", "description": "Functions whose cyclomatic complexity exceeds 10." },
    "filesOverSize": { "label": "Files over 300 LOC", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "architecture", "description": "Source files longer than 300 lines." },
    "businessRatio": { "label": "Business/boilerplate ratio", "category": "Architecture", "axis": "Change", "unit": "ratio", "direction": "higher", "collector": "architecture", "description": "Share of code that is product logic vs config/generated/barrel boilerplate. Higher = more signal per line." },

    "directDeps": { "label": "Direct dependencies", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "dependencies", "description": "Distinct third-party runtime + dev dependencies declared across the variant's manifests." },
    "circularDeps": { "label": "Circular dependencies", "category": "Architecture", "axis": "Change", "unit": "count", "direction": "lower", "collector": "dependencies", "description": "Import cycles between the variant's own projects (detected from workspace manifests)." },

    "graphNodes": { "label": "Graph nodes", "category": "Graph", "axis": "Change", "unit": "count", "direction": "balance", "collector": "graph", "description": "Projects in the variant's internal dependency graph. Scored on balance." },
    "graphEdges": { "label": "Graph edges", "category": "Graph", "axis": "Change", "unit": "count", "direction": "lower", "collector": "graph", "description": "Internal dependency edges between the variant's projects." },
    "graphDensity": { "label": "Graph density", "category": "Graph", "axis": "Change", "unit": "ratio", "direction": "lower", "collector": "graph", "description": "Edges / possible-edges — how tangled the internal graph is." },
    "fanInAvg": { "label": "Avg fan-in", "category": "Graph", "axis": "Change", "unit": "index", "direction": "neutral", "collector": "graph", "description": "Average number of projects that depend on a given project." },
    "fanOutAvg": { "label": "Avg fan-out", "category": "Graph", "axis": "Change", "unit": "index", "direction": "lower", "collector": "graph", "description": "Average number of projects a given project depends on." },
    "fanInMax": { "label": "Max fan-in", "category": "Graph", "axis": "Change", "unit": "count", "direction": "lower", "collector": "graph", "description": "Most-depended-upon project's fan-in — the blast radius of a change to the busiest hub." },
    "fanOutMax": { "label": "Max fan-out", "category": "Graph", "axis": "Change", "unit": "count", "direction": "lower", "collector": "graph", "description": "Highest number of dependencies a single project pulls in." },

    "bundleJsKb": { "label": "JS bundle (raw)", "category": "Bundle", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Total uncompressed JavaScript shipped to the browser." },
    "bundleJsGzipKb": { "label": "JS bundle (gzip)", "category": "Bundle", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Gzip-compressed JavaScript — the closest proxy to what users download." },
    "bundleJsBrotliKb": { "label": "JS bundle (brotli)", "category": "Bundle", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Brotli-compressed JavaScript." },
    "bundleCssKb": { "label": "CSS bundle (raw)", "category": "Bundle", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Total uncompressed CSS shipped to the browser." },
    "bundleCssGzipKb": { "label": "CSS bundle (gzip)", "category": "Bundle", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Gzip-compressed CSS." },
    "jsChunks": { "label": "JS chunks", "category": "Bundle", "axis": "Run", "unit": "count", "direction": "lower", "collector": "bundle", "description": "Number of emitted JavaScript chunks." },
    "cssChunks": { "label": "CSS chunks", "category": "Bundle", "axis": "Run", "unit": "count", "direction": "lower", "collector": "bundle", "description": "Number of emitted CSS chunks." },
    "largestChunkKb": { "label": "Largest chunk", "category": "Bundle", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Size of the single biggest JS chunk (a hydration-cost proxy)." },
    "imagesKb": { "label": "Images", "category": "Bundle", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Total image weight in the build output." },
    "fontsKb": { "label": "Fonts", "category": "Bundle", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Total font weight in the build output." },
    "distTotalKb": { "label": "Build artifact", "category": "Bundle", "axis": "Ship", "unit": "kb", "direction": "lower", "collector": "bundle", "description": "Total size of the deployable build output." },

    "$comment_variant_ci": "Variant-level COLD-VALIDATION metrics (collector: variant-ci, scope: variant). Produced by the measurement matrix in metrics.yml (collect-variant.mjs), which runs each variant's build/typecheck/lint/test with the build cache DISABLED (Flow: nx --skip-nx-cache; Friction/Overfit: pnpm/cargo/next outside the Nx cache). They therefore measure the INTRINSIC, cache-immune cost of the validation protocol and are directly comparable across variants — they are NOT the real cached CI feedback time (see variant.ci.feedback.duration for that). UNLIKE the repo-level GitHub metrics below (which tie across variants), these differentiate Flow / Friction / Overfit. All degrade to `unavailable` when no CI artifact is present (e.g. the default local static run).",
    "variant.ci.build.duration": { "label": "Variant build time (cold)", "category": "Cold validation", "axis": "Build", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Cache-DISABLED cold build of this variant's app(s) (Flow: nx --skip-nx-cache; Friction/Overfit: pnpm/cargo/next). Intrinsic build cost, independent of CI cache strategy. NOT the real cached CI feedback time." },
    "variant.ci.typecheck.duration": { "label": "Variant typecheck time (cold)", "category": "Cold validation", "axis": "Build", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Cache-disabled cold typecheck of this variant. Intrinsic cost, cache-immune." },
    "variant.ci.lint.duration": { "label": "Variant lint time (cold)", "category": "Cold validation", "axis": "Build", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Cache-disabled cold lint / boundary check of this variant. Intrinsic cost, cache-immune." },
    "variant.ci.test.duration": { "label": "Variant test time (cold)", "category": "Cold validation", "axis": "Build", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Cache-disabled cold unit-test run of this variant (vitest). Intrinsic cost, cache-immune." },
    "variant.ci.validation.cold": { "label": "Full validation (cold)", "category": "Cold validation", "axis": "Build", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Total cache-disabled cost of the variant's whole validation protocol: build + typecheck + lint + test, summed. The intrinsic worst-case price of one trustworthy signal." },
    "variant.ci.validation.warm": { "label": "Full re-validation (warm)", "category": "Cold validation", "axis": "Build", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "The SAME four gates re-run after the cold pass with the variant's real cache strategy in play, PRIMED first (one untimed cache-enabled run for every variant) so the number is the steady-state daily loop, not a first-run cache miss. The everyday 'is my change still green?' cost — paid dozens of times a day." },
    "variant.ci.tests.executed": { "label": "Tests executed", "category": "Build CI", "axis": "Build", "unit": "count", "direction": "neutral", "collector": "variant-ci", "scope": "variant", "description": "Number of unit tests executed by this variant's suite. Informational (not scored)." },
    "variant.ci.tests.passed": { "label": "Tests passed", "category": "Build CI", "axis": "Build", "unit": "count", "direction": "neutral", "collector": "variant-ci", "scope": "variant", "description": "Number of passing unit tests. Informational." },
    "variant.ci.tests.failed": { "label": "Tests failed", "category": "Build CI", "axis": "Build", "unit": "count", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Number of failing unit tests." },
    "variant.ci.tests.skipped": { "label": "Tests skipped", "category": "Build CI", "axis": "Build", "unit": "count", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Number of skipped unit tests." },
    "variant.ci.warnings.count": { "label": "CI warnings", "category": "Build CI", "axis": "Build", "unit": "count", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Warnings parsed from this variant's build/lint/typecheck output (heuristic across tsc/eslint/oxlint/cargo)." },
    "variant.ci.errors.count": { "label": "CI errors", "category": "Build CI", "axis": "Build", "unit": "count", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Errors parsed from this variant's build/lint/typecheck output (heuristic)." },
    "variant.ci.ramPeak.build": { "label": "Build peak RAM", "category": "Build CI", "axis": "Build", "unit": "kb", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Peak resident memory of the build step (from /usr/bin/time on the CI runner). Unavailable where the runner lacks GNU/BSD time." },
    "variant.ci.ramPeak.tests": { "label": "Test peak RAM", "category": "Build CI", "axis": "Build", "unit": "kb", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Peak resident memory of the test step (from /usr/bin/time)." },
    "variant.ci.artifact.distSize": { "label": "Variant dist size (CI)", "category": "Build CI", "axis": "Ship", "unit": "kb", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Build-output size measured in the CI job right after build. Informational — `distTotalKb` (bundle collector) is the scored artifact size." },

    "$comment_variant_feedback": "REAL cached CI feedback (collector: variant-ci, scope: variant, OBSERVATIONAL — axis:null, not scored). Mean delay from a run starting to that variant's PRIMARY quality signal being ready, WITH the variant's real cache strategy (Flow: pnpm+Nx; Overfit: 6-layer + BuildKit; Friction: pnpm store only). Definition B: max(quality_job.completed_at) − run.run_started_at (excludes the GitHub queue, which is runner-availability noise). Population: PUSH-to-main runs only — on main every variant validates its FULL scope, so samples are comparable AND free of the PR `nx affected` no-op (a cross-variant PR that triggers flow-ci with 0 affected flow projects is NOT counted). Conclusions success/failure/timed_out counted; cancelled/skipped excluded. One sample per run (latest attempt). Side jobs (Flow a11y, Overfit warm-cache) excluded via the qualityJobs allow-list. MUST be read next to the scope/size context (nxProjects, sourceFiles, locTotal, variant.ci.tests.executed): same gate categories != same amount of work. Deliberately NOT scored. `unavailable` until a real push-to-main run exists.",
    "variant.ci.feedback.duration": { "label": "CI feedback to signal (main)", "category": "CI feedback", "axis": null, "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Mean delay from a push-to-main run starting to this variant's primary quality signal (its *-ci.yml quality job), with its real cache strategy: max(quality_job.completed_at) − run.run_started_at, over push-to-main runs (conclusions success|failure|timed_out; cancelled/skipped excluded). Observational only (not scored). Interpret with nxProjects / sourceFiles / locTotal — the scopes differ (Friction 2 projects vs Flow 15 vs Overfit 31). `unavailable` until a real push-to-main run exists." },
    "variant.ci.feedback.samples": { "label": "CI feedback samples", "category": "CI feedback", "axis": null, "unit": "count", "direction": "neutral", "collector": "variant-ci", "scope": "variant", "description": "Number of push-to-main runs the feedback duration was averaged over. Informational." },

    "variant.docker.build.duration": { "label": "Docker build time", "category": "Docker", "axis": "Ship", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Wall-clock `docker build --no-cache` for this variant's image. Best-effort: `unavailable` if Docker isn't present or has no Dockerfile." },
    "variant.docker.image.size": { "label": "Docker image size", "category": "Docker", "axis": "Ship", "unit": "kb", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Final image size via `docker image inspect`. The user-facing (web) image where a variant ships several." },
    "variant.docker.layers.count": { "label": "Docker layers", "category": "Docker", "axis": "Ship", "unit": "count", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "RootFS content layers in the image — a proxy for build-cache friendliness." },
    "variant.docker.layer.maxSize": { "label": "Largest Docker layer", "category": "Docker", "axis": "Ship", "unit": "kb", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Biggest single layer (from `docker history`). Informational." },
    "variant.docker.startup.duration": { "label": "Container startup", "category": "Docker", "axis": "Ship", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Time from `docker run` to the container answering its health endpoint. Best-effort." },
    "variant.docker.healthcheck.status": { "label": "Container healthcheck", "category": "Docker", "axis": "Ship", "unit": "status", "direction": "neutral", "collector": "variant-ci", "scope": "variant", "description": "Whether the started container served its health endpoint (ok/fail). Informational." },
    "variant.docker.healthcheck.duration": { "label": "Healthcheck latency", "category": "Docker", "axis": "Ship", "unit": "ms", "direction": "lower", "collector": "variant-ci", "scope": "variant", "description": "Latency of the first successful health request once the container is up. Informational." },

    "$comment_delivery": "Deployment/diagnosis surface (collector: delivery, scope: variant). Parsed statically from each variant's REAL deployment artifacts — its Dockerfiles (the same ones release.yml ships to GHCR and docker-compose.prod.yml runs). Deterministic and variant-agnostic: a service is a Dockerfile; more services = more coordination, never more points.",
    "ship.services.count": { "label": "Services to ship", "category": "Delivery surface", "axis": "Ship", "unit": "count", "direction": "lower", "collector": "delivery", "scope": "variant", "description": "Images the variant must build, push and deploy for one release (its Dockerfiles). Every extra service is an extra build, push, deploy and coordination step." },
    "ship.healthcheck.coverage": { "label": "Guarded deploys", "category": "Delivery surface", "axis": "Ship", "unit": "%", "direction": "higher", "collector": "delivery", "scope": "variant", "description": "Share of the variant's images that declare a Docker HEALTHCHECK — deploys the platform can verify (and roll back) instead of hoping." },
    "run.inspection.surfaces": { "label": "Runtimes to inspect", "category": "Diagnosis surface", "axis": "Run", "unit": "count", "direction": "lower", "collector": "delivery", "scope": "variant", "description": "When production misbehaves, how many separate runtimes must be inspected before the fault can even be localized." },
    "run.health.coverage": { "label": "Health endpoint coverage", "category": "Diagnosis surface", "axis": "Run", "unit": "%", "direction": "higher", "collector": "delivery", "scope": "variant", "description": "Share of the variant's services that expose a dedicated health endpoint (a health route in their source, or a HEALTHCHECK that targets a health path — not just the home page)." },
    "run.structuredLogs.present": { "label": "Structured logs", "category": "Diagnosis surface", "axis": "Run", "unit": "%", "direction": "higher", "collector": "delivery", "scope": "variant", "description": "Does the runtime emit structured, leveled, contextual logs (tracing/tracing_subscriber in Rust, a leveled createLogger in TS) — or only bare console output? Signal QUALITY: one unreadable stream must not beat two well-instrumented services just because it is a single place to look." },

    "$comment_change_surface": "Contract-propagation cost (collector: change-surface, scope: variant). Measured on the shipped-everywhere `riskScore` field of the shared Signal contract: how many hand-written files under the variant's roots RE-DECLARE the field with a type (TS `riskScore: number` / Rust `pub risk_score:`). Importing the shared contract costs 0; every duplicate must be found, edited and kept in sync each time the product's data shape moves.",
    "change.contract.restatements": { "label": "Contract restatements", "category": "Change surface", "axis": "Change", "unit": "count", "direction": "lower", "collector": "change-surface", "scope": "variant", "description": "Hand-written files that re-declare the shared Signal contract (measured on the riskScore field). 0 = the variant imports the single source of truth; each extra copy is one more place a schema change must propagate to — and one more place it can silently diverge." },

    "$comment_change_footprint": "Capability FOOTPRINT (collector: change-experiment, scope: variant). The controlled experiment: the SAME product capability (Risk trend, docs/product/03-ai-task-protocol.md) implemented in all three variants against one spec. The collector measures WHERE the capability lives in each codebase today (files carrying its identifiers under the variant's roots) — a deterministic footprint, NOT a historical git diff (Overfit's implementation has no isolable baseline commit; the same footprint method is applied to all three instead of inventing a fake diff). Cross-checked against the real diffs where they exist (Flow ±0, Friction ±2 files). Deliberately REJECTED from scoring: linesChanged (no honest baseline for Overfit; rewards terseness over readability), boundariesCrossed (no variant-agnostic taxonomy survives scrutiny — projects is the honest proxy), validationCost (already Build's warm/cold gates — double count), testSupport (5/5/5 today — non-discriminating — and an exclusive source-XOR-test split biases against Rust's idiomatic inline #[cfg(test)] modules; kept as NON-exclusive context).",
    "change.footprint.sourceFiles": { "label": "Footprint — source files", "category": "Change footprint", "axis": "Change", "unit": "count", "direction": "lower", "collector": "change-experiment", "scope": "variant", "description": "Hand-written production source files that carry the Risk trend capability — the propagation surface of one identical product intent in this codebase. A footprint (where the capability lives today), not a git diff." },
    "change.footprint.testSupport": { "label": "Footprint — test support", "category": "Change footprint", "axis": "Change", "unit": "count", "direction": "neutral", "collector": "change-experiment", "scope": "variant", "description": "Test surface behind the capability: dedicated test files PLUS Rust sources with inline #[cfg(test)] modules (non-exclusive with source files). Context only — not scored: it ties across variants today and an exclusive classification would bias against Rust idioms." },
    "change.footprint.docs": { "label": "Footprint — documentation", "category": "Change footprint", "axis": "Change", "unit": "count", "direction": "lower", "collector": "change-experiment", "scope": "variant", "description": "Hand-maintained documentation files that carry the capability (ADRs, policies, governance). A synchronization surface: every copy must stay coherent when the capability moves. Not a judgment that documentation is bad — a measure of what must be kept in sync." },
    "change.footprint.projects": { "label": "Footprint — projects", "category": "Change footprint", "axis": "Change", "unit": "count", "direction": "lower", "collector": "change-experiment", "scope": "variant", "description": "Distinct workspace projects (packages/crates/apps) the capability's footprint spans — the coordination surface of one change." },
    "change.footprint.files": { "label": "Footprint — total files", "category": "Change footprint", "axis": "Change", "unit": "count", "direction": "neutral", "collector": "change-experiment", "scope": "variant", "description": "Total hand-written files carrying the capability (source + dedicated tests + docs + config). Context — the scored members are the classified counts." },
    "change.footprint.generated": { "label": "Footprint — generated files", "category": "Change footprint", "axis": "Change", "unit": "count", "direction": "neutral", "collector": "change-experiment", "scope": "variant", "description": "Generated mirrors (OpenAPI spec, generated clients) that carry the capability. Context — regenerating is cheap; the hand-written counts are what score." },

    "lighthousePerformance": { "label": "Lighthouse Performance", "category": "UX", "axis": "Run", "unit": "score", "direction": "higher", "collector": "lighthouse", "description": "Lighthouse performance score (0–100)." },
    "lighthouseAccessibility": { "label": "Lighthouse Accessibility", "category": "Accessibility", "axis": "Run", "unit": "score", "direction": "higher", "collector": "lighthouse", "description": "Lighthouse accessibility score (0–100)." },
    "lighthouseBestPractices": { "label": "Lighthouse Best Practices", "category": "UX", "axis": "Run", "unit": "score", "direction": "higher", "collector": "lighthouse", "description": "Lighthouse best-practices score (0–100)." },
    "lcpMs": { "label": "LCP", "category": "UX", "axis": "Run", "unit": "ms", "direction": "lower", "collector": "lighthouse", "description": "Largest Contentful Paint." },
    "cls": { "label": "CLS", "category": "UX", "axis": "Run", "unit": "index", "direction": "lower", "collector": "lighthouse", "description": "Cumulative Layout Shift." },
    "tbtMs": { "label": "TBT", "category": "UX", "axis": "Run", "unit": "ms", "direction": "lower", "collector": "lighthouse", "description": "Total Blocking Time." },
    "speedIndexMs": { "label": "Speed Index", "category": "UX", "axis": "Run", "unit": "ms", "direction": "lower", "collector": "lighthouse", "description": "Lighthouse Speed Index." },
    "fcpMs": { "label": "FCP", "category": "UX", "axis": "Run", "unit": "ms", "direction": "lower", "collector": "lighthouse", "description": "First Contentful Paint." },
    "requests": { "label": "Initial requests", "category": "Sustainability", "axis": "Run", "unit": "count", "direction": "lower", "collector": "lighthouse", "description": "Number of network requests to load the main route." },
    "transferredKb": { "label": "Transferred weight", "category": "Sustainability", "axis": "Run", "unit": "kb", "direction": "lower", "collector": "lighthouse", "description": "Total bytes transferred over the wire for the main route." },
    "co2PerViewMg": { "label": "CO₂ / page view", "category": "Sustainability", "axis": "Run", "unit": "mg", "direction": "lower", "collector": "lighthouse", "description": "Estimated CO₂ per page view (SWD model from transferred bytes)." },

    "$comment_github": "Repo-level delivery-pipeline metrics from the GitHub Actions API (collector: github, scope: repo). The three variants SHARE one monorepo CI pipeline, so these numbers describe the shared delivery chain — they are identical for all variants and therefore tie at 100 during min-max normalization. They enrich the Ship/Change axes with the real cost of shipping without ever faking per-variant differentiation. All degrade to `unavailable` (score falls back to the differentiating members) when no GITHUB token is present.",
    "ship.ci.wallTime.avg": { "label": "CI wall time (avg)", "category": "Delivery", "axis": "Ship", "unit": "ms", "direction": "lower", "collector": "github", "scope": "repo", "description": "Mean wall-clock time of the last N completed workflow runs (updated_at − run_started_at). Repo-level." },
    "ship.ci.wallTime.p95": { "label": "CI wall time (p95)", "category": "Delivery", "axis": "Ship", "unit": "ms", "direction": "lower", "collector": "github", "scope": "repo", "description": "95th-percentile wall-clock run time — the tail a developer occasionally waits through. Repo-level (display only)." },
    "ship.ci.successRate.lastN": { "label": "CI success rate", "category": "Delivery", "axis": "Ship", "unit": "%", "direction": "higher", "collector": "github", "scope": "repo", "description": "Successful ÷ completed runs over the last N. Repo-level." },
    "ship.ci.flakyProxy.rate": { "label": "CI instability proxy", "category": "Delivery", "axis": "Ship", "unit": "ratio", "direction": "lower", "collector": "github", "scope": "repo", "description": "Share of jobs that both passed AND failed across the sampled runs. A job-level instability proxy — NOT a test-level flaky rate. Repo-level." },
    "ship.ci.artifacts.totalSize": { "label": "CI artifacts (total)", "category": "Delivery", "axis": "Ship", "unit": "kb", "direction": "lower", "collector": "github", "scope": "repo", "description": "Total size of non-expired workflow artifacts. Repo-level." },
    "ship.deploy.avgDuration": { "label": "Deploy duration (avg)", "category": "Delivery", "axis": "Ship", "unit": "ms", "direction": "lower", "collector": "github", "scope": "repo", "description": "Average deployment duration (GitHub Deployments API, else deploy-named Actions jobs). Repo-level; unavailable if neither exists." },
    "change.pr.avgChangedFiles": { "label": "PR changed files (avg)", "category": "Pull requests", "axis": "Change", "unit": "count", "direction": "lower", "collector": "github", "scope": "repo", "description": "Average files touched per merged PR — a proxy for change surface. Repo-level, mixes all chantiers; not a controlled per-variant scenario." },
    "change.pr.avgAdditions": { "label": "PR additions (avg)", "category": "Pull requests", "axis": "Change", "unit": "loc", "direction": "lower", "collector": "github", "scope": "repo", "description": "Average lines added per merged PR. Repo-level (display only)." },
    "change.pr.avgDeletions": { "label": "PR deletions (avg)", "category": "Pull requests", "axis": "Change", "unit": "loc", "direction": "lower", "collector": "github", "scope": "repo", "description": "Average lines deleted per merged PR. Repo-level (display only)." },
    "change.pr.avgTimeToMerge": { "label": "PR time to merge (avg)", "category": "Pull requests", "axis": "Change", "unit": "ms", "direction": "lower", "collector": "github", "scope": "repo", "description": "Average created→merged duration for merged PRs — a proxy for review/merge latency. Repo-level." },
    "change.pr.avgReviewCount": { "label": "PR reviews (avg)", "category": "Pull requests", "axis": "Change", "unit": "count", "direction": "higher", "collector": "github", "scope": "repo", "description": "Average number of reviews per merged PR. Repo-level (display only)." }
  },
  "scoreGroups": {
    "$comment_axes": "The 4 delivery axes. Design rules (v2): (1) every scored member is scope:'variant' — repo-level metrics tie across variants by construction, so scoring them only dilutes real differences; they remain in the catalog as displayed context. (2) 2–6 strong members per axis beat 15 fragile ones. (3) No member rewards volume (more tests / more docs / more services / more checks): members measure either a real cost (time, count of things to coordinate) or a real guarantee (coverage of a guard).",
    "build": { "$comment": "Build = the price of one trustworthy signal on a change. Warm re-validation dominates (0.7): a developer re-validates on every change — tens of times a day — while the cold cost is paid on fresh clones and cache-less CI runs (~once a day). Both are REAL measured runs of the variant's own full gates (see variant-runner.mjs). Audited for fairness (final pass): each variant runs ITS OWN declared CI gate over its entire own scope — Flow's nx run-many over 16 projects, Friction's two apps, Overfit's TS chain + cargo (its ci:overfit gate includes clippy/test/release build by its own architectural decision). The warm pass never disables toolchain-native caches: cargo incremental and Next's cache are fully in play for Overfit — its warm≈cold ratio reflects that its stack has NO computation-cache layer (no nx equivalent), which is a real property, not a protocol artifact.", "label": "Build", "kind": "axis", "members": { "variant.ci.validation.cold": 0.3, "variant.ci.validation.warm": 0.7 } },
    "ship": { "$comment": "Ship = the price of turning a validated change into running software. Coordination surface (services to build/push/deploy) and deploy guards are static facts of the variant's real deployment artifacts; image size and no-cache image build time come from the CI matrix and gate out cleanly when Docker is absent (local static runs).", "label": "Ship", "kind": "axis", "members": { "ship.services.count": 0.3, "ship.healthcheck.coverage": 0.2, "variant.docker.image.size": 0.3, "variant.docker.build.duration": 0.2 } },
    "run": { "$comment": "Run = the price of understanding production when it misbehaves, and of restoring it. Two families: fragmentation cost (how many runtimes to inspect) and signal QUALITY at those surfaces (dedicated health endpoints, structured leveled logs) — counting only surfaces would make one unreadable log stream beat two well-instrumented services, exactly the naive metric this lab criticizes. Startup = restore speed, from the CI matrix's real container probe. Request-IDs and structured error envelopes were audited and tie across all three variants (all implement them) — a tie adds no comparison, so they are not scored. Lighthouse/browser quality is deliberately NOT here — it measures the product's local excellence, not diagnosability.", "label": "Run", "kind": "axis", "members": { "run.inspection.surfaces": 0.3, "run.health.coverage": 0.25, "run.structuredLogs.present": 0.2, "variant.docker.startup.duration": 0.25 } },
    "change": { "$comment": "Change = the price of the NEXT modification — the dominant axis, in two families. FOOTPRINT (0.60): where the same product capability lives in each codebase (the risk-trend experiment) — the measured propagation surface. STRUCTURAL (0.40): the codebase properties that EXPLAIN why it spread that way. Double-counting audit: sourceFiles and projects both grow with distribution (breadth vs coordination), combined cap 0.55 — they are the two core reads of propagation; contract restatements overlap the footprint (a duplicated contract file is also a footprint file), so restatements stay at 0.10 (down from 0.25 pre-experiment); testSupport moved to context (non-discriminating today + Rust-idiom classification bias — see $comment_change_footprint).", "label": "Change", "kind": "axis", "members": { "change.footprint.sourceFiles": 0.35, "change.footprint.projects": 0.2, "change.footprint.docs": 0.05, "change.contract.restatements": 0.1, "nxProjects": 0.08, "avgComplexity": 0.07, "businessRatio": 0.06, "anyCount": 0.04, "circularDeps": 0.03, "filesOverSize": 0.02 } },
    "uxQuality": { "label": "UX Quality", "kind": "derived", "members": { "lighthousePerformance": 0.35, "lcpMs": 0.2, "tbtMs": 0.15, "cls": 0.1, "speedIndexMs": 0.1, "fcpMs": 0.1 } },
    "accessibility": { "label": "Accessibility", "kind": "derived", "members": { "lighthouseAccessibility": 1.0 } },
    "sustainability": { "label": "Sustainability", "kind": "derived", "members": { "transferredKb": 0.4, "requests": 0.2, "co2PerViewMg": 0.2, "bundleJsGzipKb": 0.2 } },
    "architectureComplexity": { "label": "Architecture Health", "kind": "derived", "members": { "nxProjects": 0.15, "graphNodes": 0.05, "circularDeps": 0.12, "avgComplexity": 0.12, "maxComplexity": 0.08, "graphDensity": 0.05, "fanInMax": 0.1, "anyCount": 0.1, "asUnknownAsCount": 0.05, "lintDisableCount": 0.08, "businessRatio": 0.1 } }
  },
  "thresholds": { "complexity": 10, "fileSize": 300 }
}
