# packages/overfit — Variant C (Overfit) · shared packages

> ⏳ **Placeholder.** Not implemented yet. Built in the dedicated Variant C pass.

This folder will host the Overfit variant's internal packages. It currently contains only
agent configuration (`CLAUDE.md`, `AGENTS.md`, `.claude/skills`, `.agents/skills`) so that
future Overfit work is correctly scoped from day one.

Overfit must implement the **same product** as Flow (same routes, data, screens, and visible
API contract — see [`docs/product/00-product-contract.md`](../../docs/product/00-product-contract.md)).
Its complexity is deliberate and exists to measure the cost of over-engineering, not to ship
broken code. Do not start Overfit before its dedicated pass.
