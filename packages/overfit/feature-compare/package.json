{
  "name": "@signalops/overfit-feature-compare",
  "version": "0.0.0",
  "private": true,
  "description": "Overfit variant — compare feature (view models + page). Consumes the API only through the api-client.",
  "type": "module",
  "main": "./src/index.tsx",
  "types": "./src/index.tsx",
  "exports": { ".": "./src/index.tsx" },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "oxlint src"
  },
  "dependencies": {
    "@signalops/overfit-api-client": "workspace:*",
    "@signalops/overfit-contracts-generated": "workspace:*",
    "@signalops/overfit-ui": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "oxlint": "catalog:",
    "@types/node": "catalog:"
  }
}
