{
  "name": "@signalops/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and API contracts for every SignalOps variant.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "prettier --check \"src/**/*.ts\""
  }
}
