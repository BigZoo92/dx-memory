{
  "name": "@signalops/flow-effect",
  "version": "0.0.0",
  "nx": { "tags": ["scope:flow", "type:effect"] },
  "private": true,
  "description": "Flow variant — shared Effect primitives: typed Flow errors, request-id, ApiError mapping. Framework-free (effect + contracts only).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b tsconfig.json",
    "build": "tsc -b tsconfig.json",
    "test": "vitest run",
    "lint": "oxlint src"
  },
  "dependencies": {
    "@signalops/contracts": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
