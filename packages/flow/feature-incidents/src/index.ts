/**
 * @signalops/flow-feature-incidents — the `/incidents` screen.
 *
 * Summary KPI cards, status/severity/impact filters (+ reset), and the incidents table with
 * linked-signal navigation. Reads `/api/incidents` via `@signalops/flow-api-client`; incident
 * summaries computed in `@signalops/flow-domain`.
 */
export { IncidentsScreen } from './IncidentsScreen'
