/**
 * @signalops/flow-feature-compare — the `/compare` screen.
 *
 * Signal selector + re-run, the before/after attribute diff, the change timeline and the user
 * impact card. Reads `/api/compare/:id` and `/api/signals` via `@signalops/flow-api-client`.
 */
export { CompareScreen } from './CompareScreen'
