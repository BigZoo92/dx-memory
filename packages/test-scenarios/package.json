{
  "name": "@signalops/test-scenarios",
  "version": "0.0.0",
  "nx": { "tags": ["scope:shared", "type:test-scenarios"] },
  "private": true,
  "description": "Common, typed user scenarios shared by every SignalOps variant's tests.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "prettier --check \"src/**/*.ts\""
  },
  "dependencies": {
    "@signalops/contracts": "workspace:*"
  }
}
