{
  "name": "@signalops/ui-spec",
  "version": "0.0.0",
  "nx": { "tags": ["scope:shared", "type:ui-spec"] },
  "private": true,
  "description": "Typed source of truth for SignalOps design tokens, routes, screens and components.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "prettier --check \"src/**/*.ts\""
  },
  "dependencies": {
    "@signalops/contracts": "workspace:*"
  }
}
