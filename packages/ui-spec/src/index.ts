/**
 * @signalops/ui-spec
 *
 * A typed source of truth for the SignalOps UI — design tokens, routes, screen blueprints,
 * UI states, microcopy and the component catalog. This is NOT a React design system; it is
 * shared reference data so the three variants render the SAME product from the SAME spec.
 *
 * Source of truth: `docs/product/01-design-spec.md` and `maquettes/`.
 */
export * from './tokens'
export * from './routes'
export * from './screens'
export * from './components'
